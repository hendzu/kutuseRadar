# RegisterView.vue — unknown error redirect (retrofit)

Retrofit task for the already-implemented [F3 RegisterView](done/F3-register-view.md).

## Logic
- [ ] On submit: unknown/unexpected API errors (anything not explicitly handled) → `NavigationService.navigateToErrorView()`